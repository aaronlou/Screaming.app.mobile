/**
 * Entry point.
 *
 * The real application lives in `src/App.tsx`; this file exists only so that
 * `index.ts` and Expo's tooling keep finding a root `App` component.
 */
export { default } from './src/App';
