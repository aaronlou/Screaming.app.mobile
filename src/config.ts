/**
 * Build-time constants.
 *
 * Kept in one file so nothing has to import the native manifest (and the extra
 * dependency that comes with it) just to render a version string.
 */
export const APP_VERSION = '1.0.0';
